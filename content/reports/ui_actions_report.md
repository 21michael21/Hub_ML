# UI Actions Health Report

Generated: `2026-06-23T13:04:20.155331+00:00`

## Summary

- Total actions: 298
- Passing: 298
- Warnings / unsafe: 106
- Broken: 0

## Broken Actions

All UI actions have valid targets and state keys.

## Unsafe For Automated Clicking

- `tasks.run_check.python_4_advanced__cell_44` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_4_advanced__cell_45` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_4_advanced__cell_46` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_4_advanced__cell_47` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_4_advanced__cell_114` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_4_advanced__cell_115` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_4_advanced__cell_116` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_algorithms_sliding_window__manual_01` — Run task checks: Algorithms sliding window max window sum (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.stats_hypothesis_ab_conversion_rates__manual_01` — Run task checks: Hypothesis A/B test conversion rate and uplift calculation (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.career_portfolio_readme_sections__manual_01` — Run task checks: Portfolio README artifact section validator (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.career_resume_remote_bullet_structure__manual_01` — Run task checks: Resume remote career bullet structure validator (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.data_cleaning_eda_events__manual_01` — Run task checks: Data cleaning EDA missing values and duplicates (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.genai_embedding_similarity_numpy__manual_01` — Run task checks: Toy embedding similarity and vector search with NumPy (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.interview_coding_two_sum_hashmap__manual_01` — Run task checks: Coding interview mock_interview algos_patterns two sum hashmap assert task (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.interview_take_home_readme_template__manual_01` — Run task checks: ML take-home homework README template validator (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.math_linear_algebra_numpy__manual_01` — Run task checks: NumPy matrix, vector, and dot product sanity check (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.math_optimization_gradient_descent__manual_01` — Run task checks: Optimization gradient descent on a quadratic loss function (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.ml_feature_engineering_event_table__manual_01` — Run task checks: Feature engineering encoding scaling event feature table (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.ml_supervised_rule_based_baseline__manual_01` — Run task checks: Supervised learning rule-based classification baseline (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.ml_unsupervised_nearest_centroid__manual_01` — Run task checks: Unsupervised learning clustering PCA nearest centroid (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.ml_validation_metrics_manual_01` — Run task checks: Classification validation metrics from confusion counts (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.mlops_experiment_tracking_jsonl_roundtrip__manual_01` — Run task checks: Experiment tracking MLflow local JSONL roundtrip (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.mlops_monitoring_simple_drift_alert__manual_01` — Run task checks: Model monitoring data drift alert (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.mlops_serving_predict_order_conversion__manual_01` — Run task checks: Model serving API inference predict function (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.nlp_preprocessing_normalize_tokenize__manual_01` — Run task checks: NLP text preprocessing tokenization pipeline (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.nlp_vectorization_count_vectors__manual_01` — Run task checks: NLP TFIDF CountVectorizer vectorizer count vectors (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_basics_user_events__manual_01` — Run task checks: Python basics list dict loop function over user events (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_typing_architecture_normalize_event__manual_01` — Run task checks: Typed Python architecture normalize event function (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_oop_order_history__manual_01` — Run task checks: Python OOP class behavior over order history (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.sql_aggregation_windows_pandas__manual_01` — Run task checks: SQL GROUP BY and window function equivalent in pandas (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.sql_select_join_pandas_merge__manual_01` — Run task checks: SQL SELECT JOIN WHERE equivalent with pandas merge (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_2_numpy__cell_65` — Run task checks: В numpy встроены функции для поиска минимумов, максимумов и суммы. Часто мы хотим найти сумму не всего `np.array`, а по строкам или столбцам. Для этого используется аргумент `axis`. (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_2_numpy__cell_30` — Run task checks: Изменять элементы тоже легко, можно поменять одно значение, а можно сразу целую колонку. (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_2_numpy__cell_75` — Run task checks: Можно комбинировать условия с помощью `&` (и) и `|` (или). **Важно:** каждое условие обязательно брать в скобки `()`! (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_2_numpy__cell_81` — Run task checks: Можно менять форму `np.array`, главное чтобы общее количество элементов оставалось тем же. (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_3_pandas__cell_52` — Run task checks: Задание 1: Свойства и статистики (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_3_pandas__cell_55` — Run task checks: Задание 2: Фильтрация (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_3_pandas__cell_57` — Run task checks: Задание 3: Слияния (Merge) (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_3_architecture__cell_48` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_3_architecture__cell_90` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_3_architecture__cell_73` — Run task checks: Создайте класс Hero. В конструкторе он должен принимать имя и сохранять инкапсулированные параметры: `_name` (имя) и `_stamina` (выносливость, по умолчанию 100). Реализуйте следующие методы: (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_3_architecture__cell_75` — Run task checks: У вас есть базовый класс `Weapon`, который наносит урон. Создайте класс-наследник `MagicWand`. (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_3_architecture__cell_50` — Run task checks: Реализуйте методы: (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_2_basics__cell_60` — Run task checks: `+=` и `-=` иногда называют синтаксическим сахаром (сокращением записи), на самом деле они им не является, но мы пока будем считать что `b+=1` и `b=b+1` имеют одинаковый смысл. (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_2_basics__cell_77` — Run task checks: Данные можно приводить из одного типа в другой (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_5_oop__cell_10` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_5_oop__cell_19` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_1_stats__cell_12` — Run task checks: Меры центральной тенденции (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_1_stats__cell_13` — Run task checks: Меры центральной тенденции (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_1_stats__cell_7` — Run task checks: Очевидные штуки (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_1_stats__cell_8` — Run task checks: Очевидные штуки (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.analysis_1_stats__cell_9` — Run task checks: Очевидные штуки (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_6_advanced___cell_34` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_6_advanced___cell_38` — Run task checks: Задание (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_6_advanced___cell_19` — Run task checks: Напишите lambda-функцию, которая принимает одно число и возвращает его квадрат. (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_6_advanced___cell_23` — Run task checks: Ниже дан список словарей с информацией о людях. (unsafe to e2e click; target and state keys validated only)
- `tasks.run_check.python_6_advanced___cell_21` — Run task checks: Отфильтруйте список чисел так, (unsafe to e2e click; target and state keys validated only)
- `experiments.save.orders_conversion_baseline_classifier` — Save experiment record: Baseline classifier конверсии заказов (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_conversion_baseline_classifier.define_target` — Mark milestone done: Определить target (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_conversion_baseline_classifier.define_target` — Run milestone: Определить target (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_conversion_baseline_classifier.prepare_dataset` — Mark milestone done: Подготовить modeling table (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_conversion_baseline_classifier.prepare_dataset` — Run milestone: Подготовить modeling table (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_conversion_baseline_classifier.create_features` — Mark milestone done: Создать простые features (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_conversion_baseline_classifier.create_features` — Run milestone: Создать простые features (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_conversion_baseline_classifier.train_test_split` — Mark milestone done: Сделать train/test split (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_conversion_baseline_classifier.train_test_split` — Run milestone: Сделать train/test split (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_conversion_baseline_classifier.baseline_model` — Mark milestone done: Обучить baseline model (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_conversion_baseline_classifier.baseline_model` — Run milestone: Обучить baseline model (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_conversion_baseline_classifier.evaluate_metrics` — Mark milestone done: Оценить classification metrics (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_conversion_baseline_classifier.evaluate_metrics` — Run milestone: Оценить classification metrics (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_conversion_baseline_classifier.error_analysis` — Mark milestone done: Сделать базовый error analysis (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_conversion_baseline_classifier.error_analysis` — Run milestone: Сделать базовый error analysis (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_conversion_baseline_classifier.model_card` — Mark milestone done: Написать model card (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_conversion_baseline_classifier.model_card` — Run milestone: Написать model card (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.matching_quality_dashboard.inspect_matching` — Mark milestone done: Изучить matching data (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.matching_quality_dashboard.inspect_matching` — Run milestone: Изучить matching data (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.matching_quality_dashboard.check_uniqueness` — Mark milestone done: Проверить уникальность assignment (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.matching_quality_dashboard.check_uniqueness` — Run milestone: Проверить уникальность assignment (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.matching_quality_dashboard.join_orders` — Mark milestone done: Сделать join matching с orders (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.matching_quality_dashboard.join_orders` — Run milestone: Сделать join matching с orders (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.matching_quality_dashboard.quality_visuals` — Mark milestone done: Построить dashboard visuals качества (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.matching_quality_dashboard.quality_visuals` — Run milestone: Построить dashboard visuals качества (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.matching_quality_dashboard.dashboard_summary` — Mark milestone done: Написать dashboard summary (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.matching_quality_dashboard.dashboard_summary` — Run milestone: Написать dashboard summary (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_eda_report.read_context` — Mark milestone done: Разобраться в контексте датасета (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_eda_report.read_context` — Run milestone: Разобраться в контексте датасета (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_eda_report.quality_checks` — Mark milestone done: Провести базовые проверки качества (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_eda_report.quality_checks` — Run milestone: Провести базовые проверки качества (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_eda_report.core_metrics` — Mark milestone done: Посчитать основные метрики заказов (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_eda_report.core_metrics` — Run milestone: Посчитать основные метрики заказов (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_eda_report.visualize_orders` — Mark milestone done: Построить два portfolio-ready графика (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_eda_report.visualize_orders` — Run milestone: Построить два portfolio-ready графика (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.orders_eda_report.write_report` — Mark milestone done: Написать мини-отчёт (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.orders_eda_report.write_report` — Run milestone: Написать мини-отчёт (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.events_funnel_analysis.map_events` — Mark milestone done: Разобрать типы событий (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.events_funnel_analysis.map_events` — Run milestone: Разобрать типы событий (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.events_funnel_analysis.count_users` — Mark milestone done: Посчитать уникальных пользователей по шагам (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.events_funnel_analysis.count_users` — Run milestone: Посчитать уникальных пользователей по шагам (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.events_funnel_analysis.conversion_rates` — Mark milestone done: Посчитать conversion rates (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.events_funnel_analysis.conversion_rates` — Run milestone: Посчитать conversion rates (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.events_funnel_analysis.visualize_funnel` — Mark milestone done: Визуализировать funnel (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.events_funnel_analysis.visualize_funnel` — Run milestone: Визуализировать funnel (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.mark_done.events_funnel_analysis.funnel_summary` — Mark milestone done: Написать summary по funnel (unsafe to e2e click; target and state keys validated only)
- `projects.milestone.run.events_funnel_analysis.funnel_summary` — Run milestone: Написать summary по funnel (unsafe to e2e click; target and state keys validated only)
- `portfolio.export_markdown` — Export markdown (unsafe to e2e click; target and state keys validated only)
- `interviews.save_answer` — Save answer (unsafe to e2e click; target and state keys validated only)
